spring:
  datasource:
    url: jdbc:mysql://localhost:3306/company_web?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 263547
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
  mvc:
    async:
      request-timeout: 60s

server:
  tomcat:
    max-http-form-post-size: -1
    max-swallow-size: -1

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl